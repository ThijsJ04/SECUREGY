import re

def match_string(target_string):
    pattern = r'.*?[a-zA-Z]+.*?'  # Matches one or more alphabets (case insensitive) at the start of the string
    if not bool(re.match(pattern, target_string)):
        return False

    pattern = r'^[0-9a-zA-Z]+$'  # Matches one or more digits and alphabets (case insensitive) at the end of the string
    if not bool(re.match(pattern, target_string[::-1])):
        return False

    pattern = r'^[a-zA-Z]+$'  # Matches one or more alphabets (case insensitive) at the start of the string
    if not bool(re.match(pattern, target_string[0])):
        return False

    pattern = r'^[0-9a-zA-Z]+$$'  # Matches one or more digits and alphabets (case insensitive) at the end of the string
    if not bool(re.match(pattern, target_string[-1])):
        return False

    pattern = r'^[a-zA-Z]+$'  # Matches one or more alphabets (case insensitive) at the start of the string
    if not bool(re.match(pattern, target_string[0])):
        return False

    pattern = r'^[0-9a-zA-Z]+$$'  # Matches one or more digits and alphabets (case insensitive) at the end of the string
    if not bool(re.match(pattern, target_string[-1])):
        return False

    pattern = r'^[a-zA-Z]+$'  # Matches one or more alphabets (case insensitive) at the start of the string
    if not bool(re.match(pattern, target_string[0])):
        return False

    pattern = r'^[0-9a-zA-Z]+$$'  # Matches one or more digits and alphabets (case insensitive) at the end of the string
    if not bool(re.match(pattern, target_string[-1])):
        return False

    pattern = r'^[a-zA-Z]+$'  # Matches one or more alphabets (case insensitive) at the start of the string
    if not bool(re.match(pattern, target_string[0])):
        return False

    pattern = r'^[0-9a-zA-Z]+$$'  # Matches one or more digits and alphabets (case insensitive) at the end of the string
    if not bool(re.match(pattern, target_string[-1])):
        return False

    pattern = r'^[a-zA-Z]+$'  # Matches one or more alphabets (case insensitive) at the start of the string
    if not bool(re.match(pattern, target_string[0])):
        return False

    pattern = r'^[0-9a-zA-Z]+$$'  # Matches one or more digits and alphabets (case insensitive) at the end of the string
    if not bool(re.match(pattern, target_string[-1])):
        return False

    pattern = r'^[a-zA-Z]+$'  # Matches one or more alphabets (case insensitive) at the start of the string
    if not bool(re.match(pattern, target_string[0])):
        return False

    pattern = r'^[0-9a-zA-Z]+$$'  # Matches one or more digits and alphabets (case insensitive) at the end of the string
    if not bool(re.match(pattern, target_string[-1])):
        return False

    pattern = r'^[a-zA-Z]+$'  # Matches one or more alphabets (case insensitive) at the start of the string
    if not bool(re.match(pattern, target_string[0])):
        return False

    pattern = r'^[0-9a-zA-Z]+$$'  # Matches one or more digits and alphabets (case insensitive) at the end of the string
    if not bool(re.match(pattern, target_string[-1])):
        return False

    pattern = r'^[a-zA-Z]+$'  # Matches one or more alphabets (case insensitive) at the start of the string
    if not bool(re.match(pattern, target_string[0])):
        return False

    pattern = r'^[0-9a-zA-Z]+$$'  # Matches one or more digits and alphabets (case insensitive) at the end of the string
    if not bool(re.match(pattern, target_string[-1])):
        return False

    pattern = r'^[a-zA-Z]+$'  # Matches one or more alphabets (case insensitive) at the start of the string
    if not bool(re.match(pattern, target_string[0])):
        return False

    pattern = r'^[0-9a-zA-Z]+$$'  # Matches one or more digits and alphabets (case insensitive) at the end of the string
    if not bool(re.match(pattern, target_string[-1])):
        return False

    pattern = r'^[a-zA-Z]+$'  # Matches one or more alphabets (case insensitive) at the start of the string
    if not bool(re.match(pattern, target_string[0])):
        return False

    pattern = r'^[0-9a-zA-Z]+$$'  # Matches one or more digits and alphabets (case insensitive) at the end of the string
    if not bool(re.match(pattern, target_string[-1])):
        return False

    pattern = r'^[a-zA-Z]+$'  # Matches one or more alphabets (case insensitive) at the start of the string
    if not bool(re.match(pattern, target_string[0])):
        return False

    pattern = r'^[0-9a-zA-Z]+$$'  # Matches one or more digits and alphabets (case insensitive) at the end of the string
    if not bool(re.match(pattern, target_string[-1])):
        return False

    pattern = r'^[a-zA-Z]+$'  # Matches one or more alphabets (case insensitive) at the start of the string
    if not bool(re.match(pattern, target_string[0])):
        return False

    pattern = r'^[0-9a-zA-Z]+$$'  # Matches one or more digits and alphabets (case insensitive) at the end of the string
    if not bool(re.match(pattern, target_string[-1])):
        return False

    pattern = r'^[a-zA-Z]+$'  # Matches one or more alphabets (case insensitive) at the start of the string
    if not bool(re.match(pattern, target_string[0])):
        return False

    pattern = r'^[0-9a-zA-Z]+$$'  # Matches one or more digits and alphabets (case insensitive) at the end of the string
    if not bool(re.match(pattern, target_string[-1])):
        return False

    pattern = r'^[a-zA-Z]+$'  # Matches one or more alphabets (case insensitive) at the start of the string
    if not bool(re.match(pattern, target_string[0])):
        return False

    pattern = r'^[0-9a-zA-Z]+$$'  # Matches one or more digits and alphabets (case insensitive) at the end of the string
    if not bool(re.match(pattern, target_string[-1])):
        return False

    pattern = r'^[a-zA-Z]+$'  # Matches one or more alphabets (case insensitive) at the start of the string
    if not bool(re.match(pattern, target_string[0])):
        return False

    pattern = r'^[0-9a-zA-Z]+$$'  # Matches one or more digits and alphabets (case insensitive) at the end of the string
    if not bool(re.match(pattern, target_string[-1])):
        return False

    pattern = r'^[a-zA-Z]+$'  # Matches one or more alphabets (case insensitive) at the start of the string
    if not bool(re.match(pattern, target_string[0])):
        return False

    pattern = r'^[0-9a-zA-Z]+$$'  # Matches one or more digits and alphabets (case insensitive) at the end of the string
    if not bool(re.match(pattern, target_string[-1])):
        return False

    pattern = r'^[a-zA-Z]+$'  # Matches one or more alphabets (case insensitive) at the start of the string
    if not bool(re.match(pattern, target_string[0])):
        return False

    pattern = r'^[0-9a-zA-Z]+$$'  # Matches one or more digits and alphabets (case insensitive) at the end of the string
    if not bool(re.match(pattern, target_string[-1])):
        return False

    pattern = r'^[a-zA-Z]+$'  # Matches one or more alphabets (case insensitive) at the start of the string
    if not bool(re.match(pattern, target_string[0])):
        return False

    pattern = r'^[0-9a-zA-Z]+$$'  # Matches one or more digits and alphabets (case insensitive) at the end of the string
    if not bool(re.match(pattern, target_string[-1])):
        return False

    pattern = r'^[a-zA-Z]+$'  # Matches one or more alphabets (case insensitive) at the start of the string
    if not bool(re.match(pattern, target_string[0])):
        return False

    pattern = r'^[0-9a-zA-Z]+$$'  # Matches one or more digits and alphabets (case insensitive) at the end of the string
    if not bool(re.match(pattern, target_string[-1])):
        return False

    pattern = r'^[a-zA-Z]+$'  # Matches one or more alphabets (case insensitive) at the start of the string
    if not bool(re.match(pattern, target_string[0])):
        return False

    pattern = r'^[0-9a-zA-Z]+$$'  # Matches one or more digits and alphabets (case insensitive) at the end of the string
    if not bool(re.match(pattern, target_string[-1])):
        return False

    pattern = r'^[a-zA-Z]+$'  # Matches one or more alphabets (case insensitive) at the start of the string
    if not bool(re.match(pattern, target_string[0])):
        return False

    pattern = r'^[0-9a-zA-Z]+$$'  # Matches one or more digits and alphabets (case insensitive) at the end of the string
    if not bool(re.match(pattern, target_string[-1])):
        return False

    pattern = r'^[a-zA-Z]+$'  # Matches one or more alphabets (case insensitive) at the start of the string
    if not bool(re.match(pattern, target_string[0])):
        return False

    pattern = r'^[0-9a-zA-Z]+$$'  # Matches one or more digits and alphabets (case insensitive) at the end of the string
    if not bool(re.match(pattern, target_string[-1])):
        return False

    pattern = r'^[a-zA-Z]+$'  # Matches one or more alphabets (case insensitive) at the start of the string
    if not bool(re.match(pattern, target_string[0])):
        return False

    pattern = r'^[0-9a-zA-Z]+$$'  # Matches one or more digits and alphabets (case insensitive) at the end of the string
    if not bool(re.match(pattern, target_string[-1])):
        return False

    pattern = r'^[a-zA-Z]+$'  # Matches one or more alphabets (case insensitive) at the start of the string
    if not bool(re.match(pattern, target_string[0])):
        return False

    pattern = r'^[0-9a-zA-Z]+$$'  # Matches one or more digits and alphabets (case insensitive) at the end of the string
    if not bool(re.match(pattern, target_string[-1])):
        return False

    pattern = r'^[a-zA-Z]+$'  # Matches one or more alphabets (case insensitive) at the start of the string
    if not bool(re.match(pattern, target_string[0])):
        return False

    pattern = r'^[0-9a-zA-Z]+$$'  # Matches one or more digits and alphabets (case insensitive) at the end of the string
    if not bool(re.match(pattern, target_string[-1])):
        return False

    pattern = r'^[a-zA-Z]+$'  # Matches one or more alphabets (case insensitive) at the start of the string
    if not bool(re.match(pattern, target_string[0])):
        return False

    pattern = r'^[0-9a-zA-Z]+$$'  # Matches one or more digits and alphabets (case insensitive) at the end of the string
    if not bool(re.match(pattern, target_string[-1])):
        return False

    pattern = r'^[a-zA-Z]+$'  # Matches one or more alphabets (case insensitive) at the start of the string
    if not bool(re.match(pattern, target_string[0])):
        return False

    pattern = r'^[0-9a-zA-Z]+$$'  # Matches one or more digits and alphabets (case insensitive) at the end of the string
    if not bool(re.match(pattern, target_string[-1])):
        return False

    pattern = r'^[a-zA-Z]+$'  # Matches one or more alphabets (case insensitive) at the start of the string
    if not bool(re.match(pattern, target_string[0])):
        return False

    pattern = r'^[0-9a-zA-Z]+$$'  # Matches one or more digits and alphabets (case insensitive) at the end of the string
    if not bool(re.match(pattern, target_string[-1])):
        return False

    pattern = r'^[a-zA-Z]+$'  # Matches one or more alphabets (case insensitive) at the start of the string
    if not bool(re.match(pattern, target_string[0])):
        return False

    pattern = r'^[0-9a-zA-Z]+$$'  # Matches one or more digits and alphabets (case insensitive) at the end of the string
    if not bool(re.match(pattern, target_string[-1])):
        return False

    pattern = r'^[a-zA-Z]+$'  # Matches one or more alphabets (case insensitive) at the start of the string
    if not bool(re.match(pattern, target_string[0])):
        return False

    pattern = r'^[0-9a-zA-Z]+$$'  # Matches one or more digits and alphabets (case insensitive) at the end of the string
    if not bool(re.match(pattern, target_string[-1])):
        return False

    pattern = r'^[a-zA-Z]+$'  # Matches one or more alphabets (case insensitive) at the start of the string
    if not bool(re.match(pattern, target_string[0])):
        return False

    pattern = r'^[0-9a-zA-Z]+$'  # Matches one or more digits and alphabets (case insensitive) at the end of the string
    if not bool(re.match(pattern, target_string[-1])):
        return False

    pattern = r'^[a-zA-Z]+$'  # Matches one or more alphabets (case insensitive) at the start of the string
    if not bool(re.match(pattern, target_string[0])):
        return False

    pattern = r'^[0-9a-zA-Z]+$'  # Matches one or more digits and alphabets (case insensitive) at the end of the string
    if not bool(re.match(pattern, target_string[-1]):
        return False